{
  "name": "@cloud/zapier",
  "version": "1.0.0",
  "description": "Integrate Flojoy Cloud with Zapier",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "test": "pnpm run build && jest --testTimeout 10000 --rootDir ./dist",
    "clean": "rimraf ./dist ./build",
    "watch": "pnpm run clean && tsc --watch"
  },
  "engines": {
    "node": ">=v18",
    "npm": ">=5.6.0"
  },
  "dependencies": {
    "zapier-platform-core": "15.5.3"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^26.0.24",
    "@types/node": "^14.18.63",
    "jest": "^29.7.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.5",
    "zapier-platform-cli": "^15.6.1"
  },
  "zapier": {
    "convertedByCLIVersion": "15.5.1"
  }
}
