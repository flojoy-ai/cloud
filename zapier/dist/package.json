{
    "name": "flojoy-cloud",
    "version": "1.0.0",
    "description": "Integrate with Flojoy Cloud",
    "main": "dist/index.js",
    "private": true,
    "scripts": {
        "test": "npm run build && jest --testTimeout 10000 --rootDir ./dist",
        "build": "npm run clean && tsc",
        "clean": "rimraf ./dist ./build",
        "watch": "npm run clean && tsc --watch"
    },
    "engines": {
        "node": ">=v18",
        "npm": ">=5.6.0"
    },
    "dependencies": {
        "zapier-platform-core": "15.5.1"
    },
    "devDependencies": {
        "@types/jest": "^26.0.24",
        "@types/node": "^14",
        "jest": "^29.6.0",
        "rimraf": "^3.0.2",
        "typescript": "^4.9.4",
        "zapier-platform-cli": "^15.5.1"
    },
    "zapier": {
        "convertedByCLIVersion": "15.5.1"
    }
}
