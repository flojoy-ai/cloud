import { type SelectProject } from "~/types/project";

type Props = {
  project: SelectProject;
};

const DevicesView = ({ project }: Props) => {
  return <div>DevicesView</div>;
};

export default DevicesView;
