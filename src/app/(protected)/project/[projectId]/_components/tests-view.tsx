import { type SelectProject } from "~/types/project";

type Props = {
  project: SelectProject;
};

const TestsView = ({ project }: Props) => {
  return <div>TestsView</div>;
};

export default TestsView;
