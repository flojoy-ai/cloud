import { type SelectProject } from "~/types/project";

type Props = {
  project: SelectProject;
};

const UploadView = ({ project }: Props) => {
  return <div>UploadView</div>;
};

export default UploadView;
