import { type SelectProject } from "~/types/project";

type Props = {
  project: SelectProject;
};

const SettingsView = ({ project }: Props) => {
  return <div>SettingsView</div>;
};

export default SettingsView;
